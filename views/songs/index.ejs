<%- include('../partials/header') %>

    <h2>All Songs</h2>
    <a href="/playlist/new">Click here to share your favorite songs</a>
    <table>
        <thead>
            <tr id="song">
                <th>Artist</th>
                <th>Song</th>
            </tr>
        </thead>
        <table>
            <tbody>
                <% songs.forEach(function(t) { %>
                    <tr>
                        <td>
                            <%= t.artist %>
                        </td>
                        <td>
                            -
                        </td>

                        <td>
                            <em>
                                <%= t.songName %>
                            </em>
                        </td>

                    </tr>
                    <% }); %>
            </tbody>
        </table>

        <%- include('../partials/footer') %>